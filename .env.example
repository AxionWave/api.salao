# Copie para .env (docker compose) ou exporte no shell.
# Os valores DEV devem ser IGUAIS aos do oAuth / Gateway.

ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:8092

JWT_SECRET=defaultSecretKeyForJWTTokenGenerationAndValidation
JWT_ISSUER=http://localhost:8080
JWT_AUDIENCE=oauth-api

# Postgres da VPS (porta 5433 no host). Falta Host= na string.
# Ex.: Host=195.35.18.119;Port=5433;Database=base;Username=postgres;Password=...;Search Path=lyra
ConnectionStrings__Default=Host=SEU_IP;Port=5433;Database=base;Username=postgres;Password=;Search Path=lyra

EUREKA_URL=http://localhost:8761/eureka/
